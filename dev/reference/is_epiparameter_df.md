# Check if `<data.frame>` input is from [`as.data.frame.epiparameter()`](https://epiverse-trace.github.io/epiparameter/dev/reference/as.data.frame.epiparameter.md)

Check if `<data.frame>` input is from
[`as.data.frame.epiparameter()`](https://epiverse-trace.github.io/epiparameter/dev/reference/as.data.frame.epiparameter.md)

## Usage

``` r
is_epiparameter_df(x)
```

## Arguments

- x:

  A `<data.frame>`.

## Value

A single `logical` boolean.
